-- when working on java, for any edit, try use intellij mcp. If code is not
open in intellij, then dont jump to looking into jars - instead, try to
deduce the change from current projects use of required types, then from 
other open projects in intellij, then try to hallucinate a solution based
on given inputs from user and current project, and only if all that fails,
then look into jars
-- after every java edit, run console command !check - it runs checkstyle.
If not found, find it in /Users/denis.babochenko/.config/.zshrc
if some subcommand of check has compilation errors, then just run the ones that
dont have that error
-- after every change seems ok for you, commit and push
-- never run full build or tests - it takes too much time. I will run them
later manually

