{
  "$comment": "defaults for opendash instances and metadata agent/bridge; env vars win",
  "agent": "myagent",
  "metadata_refresh": 45,
  "$mcp_setup": "Set mcp_agent to an agent with Bitbucket MCP, or mcp_url to a remote bridge; see README.md"
}
