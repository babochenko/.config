{
  "$comment": "defaults for opendash instances and metadata agent/bridge; env vars win",
  "agent": "myagent",
  "metadata_refresh": 45,
  "$mcp_setup": "The metadata agent uses normal OpenCode MCP config; optionally set mcp_agent or mcp_url; see README.md"
}
